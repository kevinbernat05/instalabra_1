<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Ranking de palabras</title>
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">
</head>
<body>
    <header>
        <h1>Ranking de palabras por likes</h1>
        <nav>
            <ul>
                <li><a href="{{ path('app_home') }}">Inicio</a></li>
                {% if app.user %}
                    <li><a href="{{ path('app_perfil') }}">Mi perfil</a></li>
                    <li><a href="{{ path('app_logout') }}">Cerrar sesión</a></li>
                {% else %}
                    <li><a href="{{ path('app_login') }}">Login</a></li>
                {% endif %}
            </ul>
        </nav>
    </header>

    <main>
        <div style="margin-bottom: 20px;">
            <a href="{{ path('app_ranking', {period: 'daily'}) }}" {% if period == 'daily' %}style="font-weight:bold"{% endif %}>Diario</a> |
            <a href="{{ path('app_ranking', {period: 'weekly'}) }}" {% if period == 'weekly' %}style="font-weight:bold"{% endif %}>Semanal</a> |
            <a href="{{ path('app_ranking', {period: 'monthly'}) }}" {% if period == 'monthly' %}style="font-weight:bold"{% endif %}>Mensual</a>
        </div>

        <div style="display: flex; gap: 40px;">
            <div style="flex: 1;">
                <h2>Top Palabras</h2>
                {% if palabras|length > 0 %}
                    <ol>
                        {% for row in palabras %}
                            {% set palabra = row[0] %}
                            {% set likesCount = row.likesCount %}
                            <li>
                                <p><strong><a href="{{ path('app_usuario_perfil', {id: palabra.usuario.id}) }}">{{ palabra.usuario.nombre }}</a></strong>: {{ palabra.texto }}</p>
                                <p>👍 Likes: {{ likesCount }}</p>
                            </li>
                        {% endfor %}
                    </ol>
                {% else %}
                    <p>No hay palabras con likes todavía.</p>
                {% endif %}
            </div>

            <div style="flex: 1;">
                <h2>Top Usuarios</h2>
                {% if usuarios|length > 0 %}
                    <ol>
                        {% for row in usuarios %}
                            {% set usuario = row[0] %}
                            {% set followersCount = row.followersCount %}
                            <li>
                                <p><strong><a href="{{ path('app_usuario_perfil', {id: usuario.id}) }}">{{ usuario.nombre }}</a></strong></p>
                                <p>👥 Seguidores: {{ followersCount }}</p>
                            </li>
                        {% endfor %}
                    </ol>
                {% else %}
                    <p>No hay usuarios seguidos todavía.</p>
                {% endif %}
            </div>
        </div>
    </main>

    <footer>
        <p>&copy; 2025 Instalabra</p>
    </footer>
</body>
</html>
