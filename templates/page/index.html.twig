<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Instalabra</title>
    <link rel="stylesheet" href="{{ asset('css/app.css') }}">
</head>
<body>
    <header>
        <h1>Bienvenido a Instalabra</h1>
        <nav>
            <ul>
                <li><a href="{{ path('app_home') }}">Inicio</a></li>
                <li><a href="{{ path('app_login') }}">Login</a></li>
                <li><a href="{{ path('app_register') }}">Registro</a></li>
            </ul>
        </nav>
    </header>

    <main>
        <h2>Publica tu palabra</h2>

        {% if app.user %}
            {{ form_start(form) }}
                {{ form_row(form.texto, {'attr': {'placeholder': 'Escribe tu palabra...', 'rows': 3}}) }}
                <button type="submit">Publicar</button>
            {{ form_end(form) }}
        {% else %}
            <p>Debes <a href="{{ path('app_login') }}">iniciar sesión</a> para publicar.</p>
        {% endif %}

        <h2>Últimas palabras</h2>
            <ul>
                {% for palabra in palabras %}
                    <li>
                        <strong>{{ palabra.usuario.nombre }}</strong>: {{ palabra.texto }}

                        {# Contador de likes activo #}
                        <span>
                            ({{ palabra.valoraciones|filter(v => v.likeActiva)|length }} valoraciones)
                        </span>

                        {% if app.user %}
                            {# Comprobar si el usuario ya dio like #}
                            {% set usuarioDioLike = false %}
                            {% for valoracion in palabra.valoraciones %}
                                {% if valoracion.usuario.id == app.user.id and valoracion.likeActiva %}
                                    {% set usuarioDioLike = true %}
                                {% endif %}
                            {% endfor %}

                            {# Formulario toggle like / quitar like #}
                            <form action="{{ path('palabra_like_toggle', {'id': palabra.id}) }}" method="post" style="display:inline">
                                <button type="submit">
                                    {% if usuarioDioLike %}💔 Quitar Like{% else %}👍 Like{% endif %}
                                </button>
                            </form>
                        {% endif %}
                    </li>
                {% else %}
                    <li>No hay palabras todavía.</li>
                {% endfor %}
            </ul>
    </main>

    <footer>
        <p>&copy; 2025 Instalabra</p>
    </footer>
</body>
</html>
